using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using HighlightPlus;

public class highlightProvider : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }


    public HighlightProfile profil;

    public Material highlightPlaced, highlightSuccess;

    // Update is called once per frame
    void Update()
    {
        
    }
}
